# Unresolved mission and command questions

> **Source notice:** Public-source research only. ProjectBaseline was not read. `code_imported: false`.

## P0

Unresolved before runtime-equivalence claims: complete stock Mission catalog, aliases/defaults and case/sentinel behavior; authored Mission initialization; player/AI/Script/Trigger/internal command precedence; queue replace/append/front and mixed-selection results; typed target decoding and validation order; Guard/Area Guard/Hunt/Attack/Stop/Hold/acquire/pursuit/return; Move/Patrol/Scatter path and completion; Harvest/resource/refinery lifecycle; Enter/transport/dock; Repair/cost/ownership; Capture/post-capture; Deploy/Undeploy/state transfer; target loss, interruption, retries and follow-up commands; Script action/argument mapping and Team advancement; save/load/replay/network state; deterministic command/transition ordering.

## P1/P2

Further research: formation and waypoint patrol, aircraft/landing commands, attack-move, force fire, escort/follow, unload/passenger behavior, mission AI, stance/UI commands, extension missions/scripts, path/combat/economy adapter interfaces, runtime diagnostics and client presentation.

## Resolution discipline

Actual original runtime/source may support `ConfirmedByOriginalRuntimeSource`; FinalAlert/FinalSun only supports `ConfirmedByOfficialToolSource`; named engines/tools/clients/extensions use `ImplementationSpecificBehavior`; stable community naming uses `ConfirmedCommunityConvention`; multiple implementations require proven independent lineage or remain `Underconfirmed`; direct disagreement is `ConflictingSources`; project raw preservation/explicit requests/no execution/stable transitions are `DefensiveDesign`; insufficient evidence remains `Unresolved`.

```text
AuditStatus: NotRun
FutureEvidenceSource: ProjectBaselineAggregateAudit
```

Future aggregate observations do not close runtime questions or promote compatibility. Editor dropdowns, familiar unit behavior, one working client, animations, cursors, Unity objects and source-count voting are not valid proof.
